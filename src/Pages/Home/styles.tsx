import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  "grid-container": {},
  "grid-item": {
    paddingBottom: "10px",
  },
  "align-right": {
    textAlign: "right",
  },
});

export default useStyles;
